var app = angular.module('form', []);

app.controller('formControl', function($scope, $http) {
    var system = function($scope){
        return {
            name:name,
            href:href
        };
    }

});